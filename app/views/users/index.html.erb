<h1 class="mb-4">ユーザー一覧</h1>

<div class="list-group">
  <% @users.each do |user| %>
    <%= link_to(user.name, "/users/#{user.id}/myurl", class: "list-group-item list-group-item-action") %>
  <% end %>
</div>